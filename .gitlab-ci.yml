include:
  - project: 'deploy/dev'
    file: '/go-microservice.yml'
    ref: 'main'
  
build dev:
  extends: .build-dev

deploy dev:
  extends: .deploy-dev
  variables:
    DB_PORT: 54404
    TMP_VOLUME: tmpvol
  
build:
  extends: .build

migration-build:
  extends: .migration-build

helm-lint:
  extends: .helm-lint

deploy:
  extends: .deploy
